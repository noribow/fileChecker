# 決定すべき要件 タスクリスト

`docs/requirements.md` の「9. 未決事項」を、依存関係を考慮して着手すべき順に整理したもの。

## 1. コア処理の基本方針（最優先・他の決定のブロッカー）

- [ ] **1.1 ハッシュアルゴリズムの選定**（SHA-256 / xxHash / BLAKE3 等）
  - 破損検知用途と重複検出用途で同一アルゴリズムにするか、速度重視で分離するかも合わせて検討
  - → DBスキーマ（2.1）・重複判定方式（1.2）の前提になるため最初に決めるべき
- [ ] **1.2 重複判定アルゴリズムの方式**（フルハッシュ比較 vs サイズ＋部分ハッシュのハイブリッド）
  - 依存: 1.1（採用ハッシュの計算コスト次第で方式が変わる）

## 2. データ永続化設計

- [ ] **2.1 SQLiteスキーマ設計**（ファイル情報／整合性チェック結果／重複チェック結果／リムーバブルメディア履歴のテーブル構成）
  - 依存: 1.1, 2.2
- [ ] **2.2 リムーバブルメディアの識別方法**（ボリュームID・シリアル番号等によるドライブ同一性判定ロジック）
  - → 2.1のテーブル設計（メディア識別子カラム等）に直結するため先に方針決定が必要

## 3. お手本セット定義ファイルの外部連携

- [ ] **3.1 外部形式（CSV/XML等）のフィールドマッピング仕様**
  - 前提調査: 対象とする外部ツール（何のツールの出力を読み込みたいか）の洗い出しが先

## 4. インターフェース設計

- [ ] **4.1 CLIのサブコマンド体系・オプション設計**（整合性チェック／重複チェック／エクスポート等のコマンド構成、終了コード仕様）
- [ ] **4.2 GUIのワイヤーフレーム・画面遷移**

## 5. 横断的な方針

- [ ] **5.1 エラーハンドリング方針**（アクセス不可ファイル・権限エラー・I/Oエラー時の挙動：スキップ／リトライ／中断、ログ記録方法）
  - 他タスクと並行して検討可能

## 推奨する着手順

1.1 → 1.2 → 2.2 → 2.1 → 3.1 / 4.1 / 4.2 / 5.1（並行可）
